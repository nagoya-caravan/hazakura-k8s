---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
data:
  rewrite.conf: |
    server {
      listen 80;
    
      location / {
        add_header uri $uri;
        index /index.txt;
        try_files $uri /index.txt;
      }
    }
immutable: true
